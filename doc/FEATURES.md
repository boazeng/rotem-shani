# תכונות ושימוש

## מצלמה
- **גרירה בשטח ריק** = סיבוב · **גלגלת** = זום · **קליק-ימני + גרירה** = הזזה.
- **⬇ מבט על** — מצלמה אנכית מלמעלה (צפון למעלה). **זו גם ברירת המחדל בטעינה** — הסימולציה נפתחת במבט-על ממוסגר כך שכל המתקן נכנס עם שוליים.
- מצפן (N) בפינה שמאלית-תחתונה.

## מצב עריכה — 🔒 עדכן (בסרגל התחתון)
כברירת מחדל **נעול** (אי אפשר לשנות). לחיצה → **🔓 עדכן** (כתום = פתוח). רק במצב פתוח אפשר:
- **בחירה** — לחיצה על אובייקט (מגש/מעלית/צלחת/E1/קיר/פלורוסנט). מסגרת ירוקה + מידע בסרגל.
- **הזזה** — גרירת האובייקט הנבחר במישור X/Y.
- **סיבוב** — `Q`/`E` (5°, עם Shift 45°), או כפתורי `⟲ ⟳`; `R` = הצמדה ל-90°.
- **מחיקה** — `Delete`/`Backspace` או 🗑 מחק.
- **בניית קיר** — כפתור **➕ קיר** → לחיצה על נקודת התחלה ואז נקודת סיום. `Esc` = יציאה. הקיר גם ניתן לבחירה/הזזה/מחיקה.

> **פלורוסנטים** גם ניתנים לבחירה/הזזה/סיבוב במצב עריכה — כמו כל אובייקט.

## תפריט תצוגה (ימין עליון) — toggles
| Toggle | ברירת מחדל | פעולה |
|---|---|---|
| שרטוט (DWG) | ⬜ כבוי | שכבת השרטוט על הרצפה |
| מספרי חניה | ✅ דלוק | תוויות המספרים (**+ סמן E1** מוסתר/מוצג יחד) |
| סימון מרכזים | ⬜ כבוי | נקודות מרכז (מגשים/צלחת/מעלית) |
| קווי עזר | ⬜ כבוי | ציר המעלית × כיוון הנסיעה + נקודת הצטלבות |
| חושך מוחלט | ✅ דלוק | כיבוי אור סביבתי — רק פלורוסנטים |
| פלורוסנטים | ✅ דלוק | גופי התאורה בתקרה (אור + פסים) |
| תקרת קרקע | ⬜ כבוי | תקרת בטון חצי-שקופה |
| רכבים | ✅ דלוק | הצגת/הסתרת הרכבים |
| משטחים | ✅ דלוק | פלטות המגשים |
| **רצפת מתקן** | ✅ דלוק | רצפת הבטון. כיבוי חושף את המעלית/הבור מתחת — שימושי לבדיקה (יש **פתח חתוך ברצפה מעל המעלית** כך שהיא נראית גם כשהרצפה דלוקה) |
| רשת | ⬜ כבוי | רשת רצפה |

בסרגל התחתון גם: **🚗 הסתר רכבים** (מסונכרן עם ה-toggle).

## שמירה, אתחול, איפוס
- **💾 שמור** — כותב את הפריסה ל-`rotem_saved.json` (דרך `serve.py`). **גם קובע את מצב האתחול** (baseline).
  - בלי `serve.py`: נשמר ל-`localStorage` (נשאר ברענון, לא לקובץ).
- **↺ אתחל סימולציה** — מחזיר את כל הרכבים/אובייקטים ל**מצב האחרון ששמרת** (מהיר, בלי רענון). שימושי אחרי הרצת תרחיש.
- **איפוס** — מוחק את הפריסה השמורה וחוזר לשרטוט המקורי (עם רענון + אישור).

> **זרימת עבודה בטוחה:** סדר → 💾 שמור (במנוחה) → ▶ הרץ תרחיש → ↺ אתחל. **אל תשמור באמצע תרחיש** (זה משמר מצב מעורבב).

## תפריט זמני סימולציה (⚙, שמאל עליון)
- **מהירות נסיעה** (מ׳/ש', ברירת מחדל 2.5) — תנועת רכבים/מגשים.
- **סיבוב צלחת** (°/ש', 120) — מהירות הסיבוב על הטרנטייבל.
- **מהירות מעלית** (מ׳/ש', 1.0) — מהירות עליית המעלית.
- **מספר רכב להוצאה** + **▶ הפעל חילוץ** — הקלד מספר והרץ.

הערכים חיים (משפיעים על התרחיש הבא).

## תרחישים (▶ הדמיה / ▶ הפעל חילוץ)
- **בלוק 1** (2–11, 22): חילוץ בסגנון חידת-15 → יציאה דרך צלחת → מעלית.
- **בלוק 2** (12–20): חילוץ עם רכב 21 כעוזר → יציאה דרך E1 → צלחת → מעלית.
- **רכב 1** (המעבר, חניה P1): רכב 1 נח **מיושר לזווית המעלית (rot 97°)**, אז הוא יוצא **ישירות בלי סיבוב בצלחת**: P1 → E4 → מעלית → עלייה → הרכב נעלם → המגש הריק יורד וחוזר ל-P1. (סיבוב מתבצע רק ב**קליטה** ל-P1.)

פרטים מלאים ב-[ALGORITHMS.md](ALGORITHMS.md).

> **כיוון ומודל רכב 1:** כל הרכבים מיושרים ע"י `eastFace()` לכיוון קבוע לאורך ציר המגש — ולכן סיבוב ה-rot של המגש **לא** משנה את כיוון הרכב הנראה. כדי שרכב 1 יפנה **אל המעלית** (הפוך מברירת המחדל) יש דגל **flip פר-משטח** לרכב 1 בלבד (לא פר-מודל, כדי לא להשפיע על רכב 12 שחולק את מודל ה-barracuda), וגם תווית המספר עוברת לצד המכסה החדש.

## 🎬 הקלטת סרטון (תפריט בסרגל העליון)
מפיק סרטון של הסימולציה עם מעבר מצלמה חלק בין זוויות שהוגדרו.
- **➕ הוסף נקודת צילום** — לוכד את זווית המצלמה הנוכחית (target + radius + theta + phi) כנקודה.
- לכל נקודה **משך מעבר** (שניות) — כמה זמן המצלמה גולשת אליה מהקודמת. 👁 = קפוץ לנקודה, 🗑 = מחק.
- **▶ תצוגה** — מנגן את מעבר המצלמה **בלי** הקלטה. **⏺ הקלט** — מנגן **ומקליט**. **⏹ עצור** — עצירה ידנית.
- אופציונלי: **"הרץ חילוץ רכב N בהקלטה"** — מריץ תרחיש תוך כדי, כדי שהסרטון יתעד את התהליך.
- **מעבר חלק:** אינטרפולציה עם smoothstep; ה-theta בדרך הקצרה. לכידה דרך `canvas.captureStream` + `MediaRecorder` (ה-renderer עם `preserveDrawingBuffer`).
- **שמירה:** על השרת המאובטח — הסרטון **מועלה, מומר ל-MP4 (ffmpeg) ונשמר בשרת**; קישור מופיע בפאנל, וכפתור **📁 סרטונים שמורים** פותח את `/rec` (נגן, הורדה, מחיקה). ללא שרת (הרצה מקומית) — נופל להורדת WebM מקומית. פרטי השרת ב-[DEPLOYMENT.md](DEPLOYMENT.md).

## מודלי רכב

11 דגמים אמיתיים, כל דגם מופיע פעמיים (2×11 = 22 חניות). השיבוץ מוגדר בקוד ב-`rotem-shani.html`: המילון `TRAY_MODEL` (מספר חנייה → מפתח מודל) והמילון `MODEL_FILES` (מפתח → קובץ `.glb`).

### מיפוי מדויק — איזה מודל בכל מספר חנייה

| מפתח מודל | קובץ `.glb` | הדגם | חניות |
|---|---|---|---|
| `barracuda` | `barracuda.glb` | Plymouth Barracuda | **1, 12** |
| `hemicuda` | `hemicuda.glb` | Hemi Cuda | **2, 13** |
| `mercedes` | `mercedes.glb` | Mercedes S65 | **3, 14** |
| `golf` | `golf.glb` | Golf GTI | **4, 15** |
| `malibu` | `malibu.glb` | Chevrolet Malibu | **5, 16** |
| `car130` | `car130.glb` | "130" | **6, 17** |
| `mini` | `mini.glb` | Mini JCW | **7, 18** |
| `corvette` | `corvette.glb` | Corvette C8 | **8, 19** |
| `mustang` | `mustang.glb` | Mustang '65 | **9, 20** |
| `kona` | `kona.glb` | Hyundai Kona | **10, 21** |
| `sl63` | `sl63.glb` | Mercedes SL63 AMG | **11, 22** |

כל הקבצים נמצאים ב-`simulation/models/cars/` ומנוהלים ב-Git LFS.

**היגיון השיבוץ:** חניות 1–11 מקבלות את 11 הדגמים בזה אחר זה, וחניות 12–22 חוזרות על אותה סדרה (חנייה N וחנייה N+11 = אותו דגם).

### קבצים שקיימים אך אינם משובצים
| קובץ | סטטוס |
|---|---|
| `ferrari_plain.glb` | **fallback בלבד** — נטען לכל חנייה שאין לה מודל ב-`TRAY_MODEL` או שהמודל שלה נכשל בטעינה. כרגע כל 22 החניות משובצות, אז הוא לא נראה בפועל. |

> הערה מהקוד: דגמי `beetle`/`bmw` הושמטו (כפילות רכב בתוך אותו קובץ), ו-`oldsmobile` הושמט (שימש לכיפת סביבה). לכן הם לא ברשימה.

הוספת מודלים חדשים או שינוי שיבוץ — ראה [PIPELINE.md](PIPELINE.md); לאחר כל שינוי הרץ `.\sync.ps1` כדי שהמחשב השני יקבל אותו.
